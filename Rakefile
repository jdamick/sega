require "bundler/gem_tasks"

require_relative 'lib/sega/rake_task'

Sega::RakeTask.new() do |t|
  t.bundler_version = '1.10.6'
end

# task :my_task, [:arg1, :arg2] do |t, args|
#   puts "Args were: #{args.inspect}"
#   puts "Args were: #{args[:arg1].inspect}"
# end
